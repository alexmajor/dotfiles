- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.minttyrc: minttyrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
